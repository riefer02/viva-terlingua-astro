---
interface Props {
  message?: string;
  class?: string;
}

const { message = 'Welcome home, chiliheads!', class: className } = Astro.props;
---

<div
  class:list={[
    'w-full bg-muted px-4 py-2 text-sm font-medium text-muted-foreground text-center',
    className,
  ]}
>
  {message}
</div>
